const clock=document.querySelector('#clock');const updateClock=()=>clock.textContent=new Intl.DateTimeFormat('ru-RU',{timeZone:'Europe/Moscow',hour:'2-digit',minute:'2-digit',second:'2-digit'}).format(new Date());updateClock();setInterval(updateClock,1000);
document.querySelectorAll('.filters button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelector('.filters .active').classList.remove('active');btn.classList.add('active');document.querySelectorAll('.project').forEach(p=>p.classList.toggle('hidden',btn.dataset.filter!=='all'&&p.dataset.type!==btn.dataset.filter));if(window.ScrollTrigger)ScrollTrigger.refresh()}));
document.querySelectorAll('.process-item').forEach(item=>item.addEventListener('click',()=>{document.querySelector('.process-item.active')?.classList.remove('active');item.classList.add('active')}));document.querySelector('#top-button').onclick=()=>scrollTo({top:0,behavior:'smooth'});
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduced&&window.gsap){gsap.registerPlugin(ScrollTrigger);if(window.Lenis){const lenis=new Lenis({duration:1.1,smoothWheel:true});lenis.on('scroll',ScrollTrigger.update);gsap.ticker.add(t=>lenis.raf(t*1000));gsap.ticker.lagSmoothing(0)}gsap.from('header',{y:-70,opacity:0,duration:.9});gsap.from('.hero-title p,.hero h1 span',{y:100,opacity:0,filter:'blur(8px)',duration:1.1,stagger:.1,ease:'power4.out'});gsap.from('.hero-meta,.hero-bottom',{y:25,opacity:0,delay:.6,stagger:.15});gsap.utils.toArray('.statement h2,.about-copy h2,.why-intro,.testimonial blockquote').forEach(el=>gsap.from(el,{y:70,opacity:0,filter:'blur(7px)',duration:1,scrollTrigger:{trigger:el,start:'top 87%'}}));gsap.utils.toArray('.project,.reasons article,.process-item').forEach(el=>gsap.from(el,{y:50,opacity:0,duration:.8,scrollTrigger:{trigger:el,start:'top 90%'}}));document.querySelectorAll('[data-count]').forEach(el=>{const target=+el.dataset.count,obj={v:0};gsap.to(obj,{v:target,duration:1.5,scrollTrigger:{trigger:el,start:'top 88%'},onUpdate:()=>el.textContent=target%1?obj.v.toFixed(1):Math.round(obj.v)})});gsap.to('.progress',{scaleX:1,ease:'none',scrollTrigger:{scrub:.15,start:0,end:'max'}});const cursor=document.querySelector('.cursor');addEventListener('pointermove',e=>gsap.to(cursor,{x:e.clientX,y:e.clientY,duration:.18}));document.querySelectorAll('.project').forEach(el=>{el.onmouseenter=()=>cursor.classList.add('view');el.onmouseleave=()=>cursor.classList.remove('view')});document.querySelectorAll('.magnetic').forEach(el=>{el.onpointermove=e=>{const r=el.getBoundingClientRect();gsap.to(el,{x:(e.clientX-r.left-r.width/2)*.16,y:(e.clientY-r.top-r.height/2)*.16,duration:.2})};el.onpointerleave=()=>gsap.to(el,{x:0,y:0,duration:.6,ease:'elastic.out(1,.35)'})})}
const canvas=document.querySelector('#contact-canvas'),ctx=canvas.getContext('2d');let mx=-500,my=-500;function size(){canvas.width=canvas.offsetWidth*devicePixelRatio;canvas.height=canvas.offsetHeight*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0)}function draw(){const w=canvas.offsetWidth,h=canvas.offsetHeight;ctx.clearRect(0,0,w,h);ctx.strokeStyle='rgba(16,16,15,.22)';ctx.lineWidth=1;for(let x=0;x<w;x+=48){const pull=Math.max(0,1-Math.abs(x-mx)/220)*40;ctx.beginPath();for(let y=0;y<h;y+=12){const dy=y-my,warp=pull*Math.exp(-(dy*dy)/26000);const px=x+(x<mx?-warp:warp);y?ctx.lineTo(px,y):ctx.moveTo(px,y)}ctx.stroke()}requestAnimationFrame(draw)}canvas.addEventListener('pointermove',e=>{const r=canvas.getBoundingClientRect();mx=e.clientX-r.left;my=e.clientY-r.top});addEventListener('resize',size);size();draw();
