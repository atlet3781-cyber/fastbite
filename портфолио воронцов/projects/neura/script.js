const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
if(!reduced&&window.gsap){gsap.registerPlugin(ScrollTrigger);if(window.Lenis){const lenis=new Lenis({duration:1.2,smoothWheel:true});lenis.on('scroll',ScrollTrigger.update);gsap.ticker.add(t=>lenis.raf(t*1000));gsap.ticker.lagSmoothing(0)}gsap.from('header',{y:-70,opacity:0,duration:1,ease:'power3.out'});gsap.from('.hero h1 span,.hero h1 em',{y:120,opacity:0,filter:'blur(12px)',duration:1.2,stagger:.12,ease:'power4.out'});gsap.from('.hero .eyebrow,.hero-foot,.coordinates,.edition',{y:25,opacity:0,duration:.8,delay:.65,stagger:.1});gsap.to('.hero>img',{yPercent:12,scale:1.04,ease:'none',scrollTrigger:{trigger:'.hero',scrub:true}});gsap.utils.toArray('h2,blockquote').forEach(el=>gsap.from(el,{y:70,opacity:0,filter:'blur(8px)',duration:1,ease:'power3.out',scrollTrigger:{trigger:el,start:'top 86%'}}));gsap.utils.toArray('.project,.cap-list>div').forEach(el=>gsap.from(el,{y:70,opacity:0,scale:.98,duration:.9,ease:'power3.out',scrollTrigger:{trigger:el,start:'top 88%'}}));gsap.to('.progress',{scaleX:1,ease:'none',scrollTrigger:{scrub:.2,start:0,end:'max'}});document.querySelectorAll('[data-value]').forEach(el=>{const o={n:0},v=+el.dataset.value;gsap.to(o,{n:v,duration:1.7,scrollTrigger:{trigger:el,start:'top 90%'},onUpdate:()=>el.textContent=Math.round(o.n)})});const c=document.querySelector('.cursor');addEventListener('pointermove',e=>gsap.to(c,{x:e.clientX,y:e.clientY,duration:.15}));document.querySelectorAll('a,.project').forEach(el=>{el.onmouseenter=()=>gsap.to(c,{scale:3,duration:.2});el.onmouseleave=()=>gsap.to(c,{scale:1,duration:.2})});document.querySelectorAll('.magnetic').forEach(el=>{el.onpointermove=e=>{const r=el.getBoundingClientRect();gsap.to(el,{x:(e.clientX-r.left-r.width/2)*.14,y:(e.clientY-r.top-r.height/2)*.14,duration:.25})};el.onpointerleave=()=>gsap.to(el,{x:0,y:0,duration:.6,ease:'elastic.out(1,.35)'})})}
const canvas=document.querySelector('#field'),ctx=canvas.getContext('2d');let points=[],mouse={x:-500,y:-500};function resize(){canvas.width=canvas.offsetWidth*devicePixelRatio;canvas.height=canvas.offsetHeight*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);points=Array.from({length:55},()=>({x:Math.random()*canvas.offsetWidth,y:Math.random()*canvas.offsetHeight,vx:(Math.random()-.5)*.22,vy:(Math.random()-.5)*.22}))}function draw(){ctx.clearRect(0,0,canvas.offsetWidth,canvas.offsetHeight);points.forEach((p,i)=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>canvas.offsetWidth)p.vx*=-1;if(p.y<0||p.y>canvas.offsetHeight)p.vy*=-1;points.slice(i+1).forEach(q=>{const d=Math.hypot(p.x-q.x,p.y-q.y);if(d<130){ctx.strokeStyle=`rgba(255,59,47,${(1-d/130)*.35})`;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.stroke()}});const dm=Math.hypot(p.x-mouse.x,p.y-mouse.y);if(dm<180){ctx.strokeStyle=`rgba(255,255,255,${(1-dm/180)*.5})`;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(mouse.x,mouse.y);ctx.stroke()}ctx.fillStyle='#ff3b2f';ctx.fillRect(p.x-1,p.y-1,2,2)});requestAnimationFrame(draw)}canvas.addEventListener('pointermove',e=>{const r=canvas.getBoundingClientRect();mouse={x:e.clientX-r.left,y:e.clientY-r.top}});addEventListener('resize',resize);resize();draw();
