const fmt=n=>new Intl.NumberFormat('ru-RU').format(Math.round(n))+' ₽';const capital=document.querySelector('#capital'),monthly=document.querySelector('#monthly'),years=document.querySelector('#years');function calculate(){const c=+capital.value,m=+monthly.value,y=+years.value,r=.104/12,total=c*Math.pow(1+r,y*12)+m*((Math.pow(1+r,y*12)-1)/r);document.querySelector('#capital-label').textContent=fmt(c);document.querySelector('#monthly-label').textContent=fmt(m);document.querySelector('#years-value').textContent=y;document.querySelector('#years-label').textContent=y;document.querySelector('#future').textContent=fmt(total)}[capital,monthly,years].forEach(i=>i.addEventListener('input',calculate));calculate();
document.querySelectorAll('.currency button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelector('.currency .active').classList.remove('active');btn.classList.add('active');const b=document.querySelector('.balance');b.textContent=b.dataset[btn.dataset.currency]}));
const command=document.querySelector('.command'),scrim=document.querySelector('.scrim');function setCommand(open){command.style.display=open?'block':'none';scrim.style.display=open?'block':'none';if(open){gsap.to(command,{opacity:1,y:0,duration:.25});command.querySelector('input').focus()}}document.querySelector('.search').onclick=()=>setCommand(true);scrim.onclick=()=>setCommand(false);addEventListener('keydown',e=>{if(e.key==='/'&&!['INPUT','TEXTAREA'].includes(document.activeElement.tagName)){e.preventDefault();setCommand(true)}if(e.key==='Escape')setCommand(false)});
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduced&&window.gsap){gsap.registerPlugin(ScrollTrigger);if(window.Lenis){const l=new Lenis({duration:1.05,smoothWheel:true});l.on('scroll',ScrollTrigger.update);gsap.ticker.add(t=>l.raf(t*1000));gsap.ticker.lagSmoothing(0)}gsap.from('.hero-copy>*',{y:35,opacity:0,stagger:.09,duration:.8,ease:'power3.out'});gsap.from('.terminal',{x:70,opacity:0,rotateY:-12,duration:1.2,ease:'power4.out'});gsap.from('.line',{strokeDasharray:900,strokeDashoffset:900,duration:2,delay:.5});gsap.utils.toArray('h2,.feature-workspace,.calc,.vault,.price-card').forEach(el=>gsap.from(el,{y:55,opacity:0,duration:.9,ease:'power3.out',scrollTrigger:{trigger:el,start:'top 86%'}}));gsap.to('.topline',{position:'fixed',zIndex:100,width:'100%',scrollTrigger:{trigger:'body',start:'top top'}})}
